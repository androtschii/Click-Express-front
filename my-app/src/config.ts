export const API_BASE = import.meta.env.VITE_API_BASE_URL ?? "http://localhost:5114/api";

// Google OAuth client id — set VITE_GOOGLE_CLIENT_ID in .env.local (see .env.example).
export const GOOGLE_CLIENT_ID = import.meta.env.VITE_GOOGLE_CLIENT_ID ?? "";
